@model CheckoutCompletedModel
@using Nop.Web.Models.Checkout;
@{

    //title
    Html.AddTitleParts(T("PageTitle.Checkout").Text);
}

@section Left{

    @*@Html.Partial("MenuStatic")*@

    @*@if (!Model.OnePageCheckoutEnabled)
    {
        @Html.Action("CheckoutProgress", "Checkout", new { step = CheckoutProgressStep.Complete })
    }*@

}

@functions{
    public string FormatOrderId(int orderId)
    {
        if (orderId > 99999999)
            return orderId.ToString();
        else
            return orderId.ToString("D5");
    }
}


    <section class="col-main-sml checkout-completed-blox mt-large text-center">

        @*<div class="row">
        <div class="col-5">
            <img src="~/Content/Images/uploaded/assets/completedpage.jpg" class="img-fluid mx-auto d-block" />
        </div>
        <div class="col-7">*@
        <p style="color:#969696; text-transform:uppercase; margin-top:40px;">
            @*@T("Checkout.YourOrderHasBeenSuccessfullyProcessed")*@
           Thank you for your order
        </p>

        <p class="mt-4" style="font-size:1.5rem;">
            Your Order# <span class="checkout-completed-orderid"><a href="@Url.RouteUrl("OrderDetails", new { orderID = Html.AttributeEncode(Model.OrderId) })">@FormatOrderId(Model.OrderId)</a></span>
        </p>

        <p class="mt-4">If you choose to pay via Bank transfer, please make your payment directly to the following bank account and include your order number as the payment reference.<br />
            <strong>BCA</strong><br />Account No.: 0860.656.493<br />Beneficiary Name: Ilham Rahmandika
        </p>
        <p class="text-center mt-4">
            We are getting started on your order right away, and you will receive an order confirmation email shortly to your email.
        </p>


        @*<p class="text-center mt-4">
            <a href="@Url.RouteUrl("OrderDetails", new { orderID = Html.AttributeEncode(Model.OrderId) })">@T("Checkout.PlacedOrderDetails")</a>
            <div class="buttons">
                <input type="button" value="@T("Checkout.ThankYou.Continue")" class="btn btn-default" onclick="setLocation('@Url.RouteUrl("HomePage")')" />
            </div>
        </p>*@

        <p style="color:#969696; text-transform:uppercase;">
            @T("Checkout.ThankYou")
        </p>

        @*</div>
        </div>*@
    </section>
<br /><br /><br /><br /><br />




@section scripts {
    <script>
        $(document).ready(function () {
            function disableBack() { window.history.forward() }
            window.onload = disableBack();
            window.onpageshow = function (evt) { if (evt.persisted) disableBack() }
        });
    </script>
}



