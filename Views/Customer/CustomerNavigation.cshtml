@model CustomerNavigationModel
@using Nop.Web.Models.Customer;




<div class="list-group">

    <ul class="list-unstyled">

        <li class="list-group-item">
            <a href="@Url.Action("MyAccount")" class="@if (Model.SelectedTab == CustomerNavigationEnum.MyAccount) { <text> active</text>}
               else
               {<text>inactive</text>}">@T("Account.Navigation")</a>
        </li>


     

        @if (!Model.HideInfo)
        {
            <li class="list-group-item">
                <a href="@Url.RouteUrl("CustomerInfo")" class="@if (Model.SelectedTab == CustomerNavigationEnum.Info)
                                                                   {<text>active</text>}
                                                                   else
                                                                   {<text>inactive</text>}">Personal information@*@T("Account.CustomerInfo")*@</a>
                                                            </li>
        }
        @if (!Model.HideAddresses)
        {
            <li class="list-group-item">
                <a href="@Url.RouteUrl("CustomerAddresses")" class="@if (Model.SelectedTab == CustomerNavigationEnum.Addresses)
                                                                        {<text>active</text>}
                                                                        else
                                                                        {<text>inactive</text>}">Address book@*@T("Account.CustomerAddresses")*@</a>
                                                                    </li>
        }

        @if (!Model.HideChangePassword)
        {
            <li class="list-group-item">
                <a href="@Url.RouteUrl("CustomerChangePassword")" class="@if (Model.SelectedTab == CustomerNavigationEnum.ChangePassword)
                                                                             {<text>active</text>}
                                                                             else
                                                                             {<text>inactive</text>}">@T("Account.ChangePassword")</a>
                                                                        </li>
        }

        @if (!Model.HideOrders)
        {
            <li class="list-group-item">
                <a href="@Url.RouteUrl("CustomerOrders")" class="@if (Model.SelectedTab == CustomerNavigationEnum.Orders)
                                                                     {<text>active</text>}
                                                                     else
                                                                     {<text>inactive</text>}">@T("Account.CustomerOrders")</a>
                                                                </li>
        }
        @if (!Model.HideReturnRequests)
        {
            <li class="list-group-item">
                <a href="@Url.RouteUrl("CustomerReturnRequests")" class="@if (Model.SelectedTab == CustomerNavigationEnum.ReturnRequests)
                                                                             {<text>active</text>}
                                                                             else
                                                                             {<text>inactive</text>}">@T("Account.CustomerReturnRequests")</a>
                                                                        </li>
        }

    

        @*@if (!Model.HideBackInStockSubscriptions)
        {
            <li class="list-group-item">
                <a href="@Url.RouteUrl("CustomerBackInStockSubscriptions")" class="@if (Model.SelectedTab == CustomerNavigationEnum.BackInStockSubscriptions)
                                                                                   {<text>active</text>}
                                                                                   else
                                                                                   {<text>inactive</text>}">@T("Account.BackInStockSubscriptions")</a>
                                                                            </li>
        }*@

     

        <li class="list-group-item">
            <a href="~/Order/ConfirmPayments" class="@if (Model.SelectedTab == CustomerNavigationEnum.ConfirmPayment)
                                                                                 {<text>active</text>}
                                                                                 else
                                                                                 {<text>inactive</text>}">Confirm payment</a>
        </li>


        @*<li class="list-group-item">
            <ul class="list-unstyled sub-cat">

                @if (!Model.HideAvatar)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerAvatar")" class="@if (Model.SelectedTab == CustomerNavigationEnum.Avatar)
                                                                     {<text>active</text>}
                                                                     else
                                                                     {<text>inactive</text>}">@T("Account.Avatar")</a>
                                                                </li>
                }

                @if (!Model.HideInfo)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerInfo")" class="@if (Model.SelectedTab == CustomerNavigationEnum.Info)
                                                                   {<text>active</text>}
                                                                   else
                                                                   {<text>inactive</text>}">@T("Account.CustomerInfo")</a>
                                                            </li>
                }
                @if (!Model.HideAddresses)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerAddresses")" class="@if (Model.SelectedTab == CustomerNavigationEnum.Addresses)
                                                                        {<text>active</text>}
                                                                        else
                                                                        {<text>inactive</text>}">@T("Account.CustomerAddresses")</a>
                                                                    </li>
                }

                @if (!Model.HideChangePassword)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerChangePassword")" class="@if (Model.SelectedTab == CustomerNavigationEnum.ChangePassword)
                                                                             {<text>active</text>}
                                                                             else
                                                                             {<text>inactive</text>}">@T("Account.ChangePassword")</a>
                                                                        </li>
                }

                @if (!Model.HideOrders)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerOrders")" class="@if (Model.SelectedTab == CustomerNavigationEnum.Orders)
                                                                     {<text>active</text>}
                                                                     else
                                                                     {<text>inactive</text>}">@T("Account.CustomerOrders")</a>
                                                                </li>
                }
                @if (!Model.HideReturnRequests)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerReturnRequests")" class="@if (Model.SelectedTab == CustomerNavigationEnum.ReturnRequests)
                                                                             {<text>active</text>}
                                                                             else
                                                                             {<text>inactive</text>}">@T("Account.CustomerReturnRequests")</a>
                                                                        </li>
                }

                @if (!Model.HideDownloadableProducts)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerDownloadableProducts")" class="@if (Model.SelectedTab == CustomerNavigationEnum.DownloadableProducts)
                                                                                   {<text>active</text>}
                                                                                   else
                                                                                   {<text>inactive</text>}">@T("Account.DownloadableProducts")</a>
                                                                            </li>
                }
              
                <li class="list-group-item">
                    <a href="~/customer/giftcards" class="@if (Model.SelectedTab == CustomerNavigationEnum.GiftCards)
                                                                           {<text>active</text>}
                                                                           else
                                                                           {<text>inactive</text>}">Voucher</a>
                </li>

                @if (!Model.HideRewardPoints)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerRewardPoints")" class="@if (Model.SelectedTab == CustomerNavigationEnum.RewardPoints)
                                                                           {<text>active</text>}
                                                                           else
                                                                           {<text>inactive</text>}">@T("Account.RewardPoints")</a>
                                                                    </li>
                }

                @if (!Model.HideBackInStockSubscriptions)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerBackInStockSubscriptions")" class="@if (Model.SelectedTab == CustomerNavigationEnum.BackInStockSubscriptions)
                                                                                   {<text>active</text>}
                                                                                   else
                                                                                   {<text>inactive</text>}">@T("Account.BackInStockSubscriptions")</a>
                                                                            </li>
                }

                @if (!Model.HideForumSubscriptions)
                {
                <li class="list-group-item">
                    <a href="@Url.RouteUrl("CustomerForumSubscriptions")" class="@if (Model.SelectedTab == CustomerNavigationEnum.ForumSubscriptions)
                                                                                 {<text>active</text>}
                                                                                 else
                                                                                 {<text>inactive</text>}">@T("Account.ForumSubscriptions")</a>
                                                                            </li>
                }

                <li class="list-group-item">
                    <a href="~/Order/ConfirmPayment" class="@if (Model.SelectedTab == CustomerNavigationEnum.ConfirmPayment)
                                                                                 {<text>active</text>}
                                                                                 else
                                                                                 {<text>inactive</text>}">Confirm payment</a>
                </li>
            </ul>
        </li>*@
    </ul>

</div>

<div class="list-group back-to-shop">
    <ul class="list-unstyled">
        <li class="list-group-item"><a href="~/">Back to shop</a></li>
    </ul>
</div>